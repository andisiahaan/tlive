{
  "name": "tlive",
  "version": "1.0.0",
  "description": "TikTok Live Interactive Platform",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend && cd backend && npx prisma db push",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "author": "TLive Contributors",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
